data:
  ratings_path: data/ratings.csv
  user_col: userId
  item_col: movieId
  rating_col: rating
  timestamp_col: timestamp

train:
  model_type: als
  seed: 42
  als_rank: 64
  als_reg: 0.01
  als_iters: 15
  top_k: 10

eval:
  top_k: 10